apply plugin: "android-library"

android {
    compileSdkVersion 3
    buildToolsVersion "19.0.3"

    buildTypes {
        release {
            runProguard false
            proguardFiles getDefaultProguardFile("proguard-android.txt"), "parcelgen-runtime/proguard.cfg"
        }
    }
    sourceSets {
        main {
            manifest.srcFile "parcelgen-runtime/AndroidManifest.xml"
            java.srcDirs = ["parcelgen-runtime/src"]
            resources.srcDirs = ["parcelgen-runtime/src"]
            res.srcDirs = ["parcelgen-runtime/res"]
        }
    }
}
